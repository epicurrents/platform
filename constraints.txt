# Minimum-version constraints for transitive dependencies with known CVEs.
# Constraints never install anything; they only bound the resolver when a
# listed package is pulled in by something else. Sourced from the
# 2026-07-18 security sweep (pip-audit in CI).
cryptography>=50.0.0
urllib3>=2.7.0
aiohttp>=3.14.3
idna>=3.18
pytest>=9.1.1
