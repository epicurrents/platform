"""Federation app — inter-instance federated data sharing.

Provides ``FederatedPeer`` model, Ed25519 JWT authentication, well-known
public-key endpoint, peer and grant management API, and the optional FUSE
virtual filesystem for mounting remote recordings as local files.
"""
