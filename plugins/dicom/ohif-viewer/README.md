# OHIF Viewer (submodule placeholder)

This directory is a git submodule pointing to https://github.com/OHIF/Viewers.git.

Run the following to initialise it:

    git submodule update --init --checkout plugins/dicom/ohif-viewer

Then build and deploy the viewer with:

    ./scripts/build_ohif.sh
