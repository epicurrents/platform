-c constraints.txt
# Test-only Python dependencies.
#
# Kept separate from requirements.txt so the manifest is honest about which
# deps are production runtime vs. which only run during pytest.  The Dockerfile
# installs these in its `test` stage only, so nothing here reaches the runtime
# image the deployment runs; the `test` and `test-postgres` compose services
# name that stage as their build target.
#
# Add CI / lint dependencies (ruff, pip-audit) to requirements-dev.txt
# instead — those run on the host, not in the test container.

pytest>=9.1.1
pytest-django>=4.13.0
pytest-cov>=5.0
pytest-httpserver>=1.1
model-bakery>=1.24.0

# Borgmatic ships PyYAML; the host venv needs it to exercise
# borgmatic/emit_runtime_config.py in pytest.
PyYAML>=6.0.3
